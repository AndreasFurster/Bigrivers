@{
    ViewBag.Title = "Optredens";
}
<h1>Optredens</h1>
<ul>
    @foreach (Performance p in ViewBag.PerformancesList)
    {
        <li>
            @Html.ActionLink(p.Description, "Optreden", new { Id = p.Id })
        </li>
    }
</ul>